#
# /etc/hosts: static lookup table for host names
#
# {{ ansible_managed }}

#<ip-address>	<hostname.domain.org>	<hostname>
127.0.0.1	localhost.localdomain	localhost  {{ inventory_hostname }}
::1		localhost.localdomain	localhost  {{ inventory_hostname }}

# End of file
# vim : set ts=8 sw=8 noet :
